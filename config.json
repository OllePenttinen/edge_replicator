{
  "title": "Function Replicator configuration",
  "type": "object",
  "properties": {
    "destination_installation": {
      "type": "string",
      "title": "Destination installation ID",
      "description": "The installation to replicate functions to"
    },
    "function_patterns": {
      "type": "array",
      "title": "Function patterns",
      "description": "List of function IDs or wildcards to watch",
      "items": { "type": "string" },
      "default": ["sensor.*", "relay.*"]
    }
  },
  "required": ["destination_installation"]
}