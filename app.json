{
  "title": "Function Replicator",
  "description": "Mirrors functions from source to destination installation based on wildcard patterns",
  "input": {
    "destination_installation": {
      "required": true,
      "type": "string",
      "name": "Destination Installation ID",
      "description": "ID of the target installation where matching functions will be created or deleted"
    },
    "api_key": {
      "required": true,
      "type": "string",
      "name": "API Key for Destination Installation",
      "description": "A user API key with permission to create or delete functions in the destination installation"
    }//,
    // "function_patterns": {
    //   "required": true,
    //   "type": "array",
    //   "name": "Function patterns to monitor",
    //   "description": "Wildcard patterns (using * and ?) for functions to replicate",
    //   "items": { "type": "string" },
    //   "default": ["sensor.*", "relay.*"]
    //}
  },
  "guide": [
    {
      "title": "Parameter selection",
      "description": "Set the parameters you want to use.",
      "input_fields": [
		  "destination_installation",
		  "api_key"//,
		  //"function_patterns"
      ]
    }
  ]
}  
